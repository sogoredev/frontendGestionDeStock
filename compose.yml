services:
  stock-front:
    image: docker.io/sogoredev/stock-front:1.0.0
    container_name: stock-front
    restart: always
    networks:
      - applications-network

networks:
  applications-network:
    external: true
    name: applications-network
