services:
  stock-front:
    image: sogoredev/stock-front:1.0.0
    container_name: stock-front
    user: nginx
    restart: always
    networks:
      - applications-network
networks:
  applications-network:
    external: true
    name: applications-network
